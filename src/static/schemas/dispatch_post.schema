{
  "type": "object",
  "required": [ "targetId" ],
  "properties": {
    "targetId": {
      "description": "ID of target to retry",
      "type": "string",
      "format": "uuid",
      "minLength": 1
    }
  }
}